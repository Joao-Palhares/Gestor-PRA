/*
Deployment script for GestorDB

This code was generated by a tool.
Changes to this file may cause incorrect behavior and will be lost if
the code is regenerated.
*/

GO
SET ANSI_NULLS, ANSI_PADDING, ANSI_WARNINGS, ARITHABORT, CONCAT_NULL_YIELDS_NULL, QUOTED_IDENTIFIER ON;

SET NUMERIC_ROUNDABORT OFF;


GO
:setvar DatabaseName "GestorDB"
:setvar DefaultFilePrefix "GestorDB"
:setvar DefaultDataPath "C:\Users\jonip\AppData\Local\Microsoft\Microsoft SQL Server Local DB\Instances\MSSQLLocalDB\"
:setvar DefaultLogPath "C:\Users\jonip\AppData\Local\Microsoft\Microsoft SQL Server Local DB\Instances\MSSQLLocalDB\"

GO
:on error exit
GO
/*
Detect SQLCMD mode and disable script execution if SQLCMD mode is not supported.
To re-enable the script after enabling SQLCMD mode, execute the following:
SET NOEXEC OFF; 
*/
:setvar __IsSqlCmdEnabled "True"
GO
IF N'$(__IsSqlCmdEnabled)' NOT LIKE N'True'
    BEGIN
        PRINT N'SQLCMD mode must be enabled to successfully execute this script.';
        SET NOEXEC ON;
    END


GO
USE [$(DatabaseName)];


GO
PRINT N'Creating [dbo].[sp_InsertMedidas]...';


GO
CREATE PROCEDURE [dbo].[sp_InsertMedidas]
	@id_medidas int,
	@periodo_inicio datetime,
	@periodo_fim datetime,
	@medida varchar(300),
	@cumprimento varchar(50),
	@data_cumprimento date,
	@dever_incumprimento varchar(50),
	@data_incumprimento date,
	@faltas_desconsideradas varchar(300)

AS
Begin
	Declare @count int
	SELECT @count= COUNT(id_medidas) FROM tblPraMedidas Where id_medidas=@id_medidas

	IF(@count<>0)
	begin 
		select -1 as ReturnCode4
	End
	Begin
		Insert INTO [dbo].[tblPraMedidas]
				([id_medidas],
				[periodo_inicio],
				[periodo_fim],
				[medida],
				[cumprimento],
				[data_cumprimento],
				[data_incumprimento],
				[dever_incumprimento],
				[data_incumprimento],
				[faltas_desconsideradas])
		Values (@id_medidas,@periodo_inicio,@periodo_fim,@medida,@cumprimento,@data_cumprimento,@dever_incumprimento,@data_incumprimento,@faltas_desconsideradas)
		select 1 as ReturnCode4
	END
END
GO
PRINT N'Creating [dbo].[sp_InsertNotificacoes]...';


GO
CREATE PROCEDURE [dbo].[sp_InsertNotificacoes]
	@id_notificaçoes int,
	@assinatura_enc varchar(50),
	@data_assinatura_enc date,
	@assinatura_dt varchar(50),
	@data_assinatura_dt date,
	@assinatura_pt varchar(50),
	@data_assinatura_pt date,
	@data_assinatura_cpcj date

AS
Begin
	Declare @count int
	SELECT @count= COUNT(id_notificaçoes) FROM tblPraNotificacoes Where id_notificaçoes=@id_notificaçoes

	IF(@count<>0)
	begin 
		select -1 as ReturnCode4
	End
	Begin
		Insert INTO [dbo].[tblPraNotificacoes]
				([id_notificaçoes],
				[assinatura_enc],
				[data_assinatura_enc],
				[assinatura_dt],
				[data_assinatura_dt],
				[assinatura_pt],
				[data_assinatura_pt],
				[data_assinatura_cpcj])
		Values (@id_notificaçoes,@assinatura_enc,@data_assinatura_enc,@assinatura_dt,@data_assinatura_dt,@assinatura_pt,@data_assinatura_pt,@data_assinatura_cpcj)
		select 1 as ReturnCode4
	END
END
GO
PRINT N'Creating [dbo].[sp_InsertPra]...';


GO
CREATE PROCEDURE [dbo].[sp_InsertPra]
	@id_pra int,
	@id_principal int,
	@id_dm int,
	@id_medidas int,
	@id_notificaçoes int,
	@id_decisao int,
	@codigo_pra varchar(20)

AS
Begin
	Declare @count int
	SELECT @count= COUNT(id_pra) FROM tblPra Where id_pra=@id_pra

	IF(@count<>0)
	begin 
		select -1 as ReturnCodepra
	End
	Begin
		Insert INTO [dbo].[tblPra]
				([id_pra],
				[id_principal],
				[id_dm],
				[id_medidas],
				[id_notificaçoes],
				[id_decisao],
				[codigo_pra])
		Values (@id_pra,@id_principal,@id_dm,@id_medidas,@id_notificaçoes,@id_decisao,@codigo_pra)
		select 1 as ReturnCodepra
	END
END
GO
PRINT N'Creating [dbo].[sp_InsertPrh]...';


GO
CREATE PROCEDURE [dbo].[sp_InsertPrh]
	@id_prh int,
	@id_principal int,
	@id_avaliaçoes int,
	@codigo_prh varchar(20)

AS
Begin
	Declare @count int
	SELECT @count= COUNT(id_prh) FROM tblPrh Where id_prh=@id_prh

	IF(@count<>0)
	begin 
		select -1 as ReturnCode
	End
	Begin
		Insert INTO [dbo].[tblPrh]
				([id_prh],
				[id_principal],
				[id_avaliaçoes])
		Values (@id_prh,@id_principal,@id_avaliaçoes)
		select 1 as ReturnCode
	END
END
GO
PRINT N'Creating [dbo].[sp_InsertPrhPrincipal]...';


GO
CREATE PROCEDURE [dbo].[sp_InsertPrhPrincipal]
	@id_principal int,
	@ano_letivo char(13),
	@turma varchar(100),
	@numero_aluno int,
	@nome_aluno varchar(200),
	@curso varchar(200),
	@disciplina varchar(50),
	@tempo_letivos_faltas int,
	@modalidade_adotada varchar(400),
	@outra_modalidade varchar(100)

AS
Begin
	Declare @count int
	SELECT @count= COUNT(id_principal) FROM tblPrhPrincipal Where id_principal=id_principal

	IF(@count<>0)
	begin 
		select -1 as ReturnCode1
	End
	Begin
		Insert INTO [dbo].[tblPrhPrincipal]
				([id_principal],
				[ano_letivo],
				[turma],
				[numero_aluno],
				[nome_aluno],
				[curso],
				[disciplina],
				[tempo_letivos_faltas],
				[modalidade_adotada],
				[outra_modalidade])
		Values (@id_principal,@ano_letivo,@turma,@numero_aluno,@nome_aluno,@curso,@disciplina,@tempo_letivos_faltas,@modalidade_adotada,@outra_modalidade)
		select 1 as ReturnCode1
	END
END
GO
PRINT N'Creating [dbo].[sp_InsertPrincipal]...';


GO
CREATE PROCEDURE [dbo].[sp_InsertPrincipal]
	@id_principal int,
	@nome_aluno varchar(200),
	@idade int,
	@ano_letivo char(13),
	@turma varchar(100),
	@numero_aluno int

AS
Begin
	Declare @count int
	SELECT @count= COUNT(id_principal) FROM tblPraPrincipal Where id_principal=@id_principal

	IF(@count<>0)
	begin 
		select -1 as ReturnCode
	End
	Begin
		Insert INTO [dbo].[tblPraPrincipal]
				([id_principal],
				[nome_aluno],
				[idade],
				[ano_letivo],
				[turma],
				[numero_aluno])
		Values (@id_principal,@nome_aluno,@idade,@ano_letivo,@turma,@numero_aluno)
		select 1 as ReturnCode
	END
END
GO
PRINT N'Update complete.';


GO
