/*
Deployment script for GestorDB

This code was generated by a tool.
Changes to this file may cause incorrect behavior and will be lost if
the code is regenerated.
*/

GO
SET ANSI_NULLS, ANSI_PADDING, ANSI_WARNINGS, ARITHABORT, CONCAT_NULL_YIELDS_NULL, QUOTED_IDENTIFIER ON;

SET NUMERIC_ROUNDABORT OFF;


GO
:setvar DatabaseName "GestorDB"
:setvar DefaultFilePrefix "GestorDB"
:setvar DefaultDataPath "C:\Users\jonip\AppData\Local\Microsoft\Microsoft SQL Server Local DB\Instances\MSSQLLocalDB"
:setvar DefaultLogPath "C:\Users\jonip\AppData\Local\Microsoft\Microsoft SQL Server Local DB\Instances\MSSQLLocalDB"

GO
:on error exit
GO
/*
Detect SQLCMD mode and disable script execution if SQLCMD mode is not supported.
To re-enable the script after enabling SQLCMD mode, execute the following:
SET NOEXEC OFF; 
*/
:setvar __IsSqlCmdEnabled "True"
GO
IF N'$(__IsSqlCmdEnabled)' NOT LIKE N'True'
    BEGIN
        PRINT N'SQLCMD mode must be enabled to successfully execute this script.';
        SET NOEXEC ON;
    END


GO
USE [$(DatabaseName)];


GO
/*
The column [dbo].[tblDecisao].[decisao] is being dropped, data loss could occur.
*/

IF EXISTS (select top 1 1 from [dbo].[tblDecisao])
    RAISERROR (N'Rows were detected. The schema update is terminating because data loss might occur.', 16, 127) WITH NOWAIT

GO
PRINT N'Dropping [dbo].[FK_tblPra_tblDecisao]...';


GO
ALTER TABLE [dbo].[tblPra] DROP CONSTRAINT [FK_tblPra_tblDecisao];


GO
PRINT N'Starting rebuilding table [dbo].[tblDecisao]...';


GO
BEGIN TRANSACTION;

SET TRANSACTION ISOLATION LEVEL SERIALIZABLE;

SET XACT_ABORT ON;

CREATE TABLE [dbo].[tmp_ms_xx_tblDecisao] (
    [id_decisao]              INT           IDENTITY (1, 1) NOT NULL,
    [data_conselho]           DATE          NOT NULL,
    [consequencia]            VARCHAR (100) NOT NULL,
    [data_eadpf]              DATE          NULL,
    [medidas_c_s]             VARCHAR (100) NULL,
    [assinatura_diretor]      VARCHAR (50)  NOT NULL,
    [data_assinatura_diretor] DATE          NOT NULL,
    PRIMARY KEY CLUSTERED ([id_decisao] ASC)
);

IF EXISTS (SELECT TOP 1 1 
           FROM   [dbo].[tblDecisao])
    BEGIN
        SET IDENTITY_INSERT [dbo].[tmp_ms_xx_tblDecisao] ON;
        INSERT INTO [dbo].[tmp_ms_xx_tblDecisao] ([id_decisao], [data_conselho], [consequencia], [assinatura_diretor], [data_assinatura_diretor])
        SELECT   [id_decisao],
                 [data_conselho],
                 [consequencia],
                 [assinatura_diretor],
                 [data_assinatura_diretor]
        FROM     [dbo].[tblDecisao]
        ORDER BY [id_decisao] ASC;
        SET IDENTITY_INSERT [dbo].[tmp_ms_xx_tblDecisao] OFF;
    END

DROP TABLE [dbo].[tblDecisao];

EXECUTE sp_rename N'[dbo].[tmp_ms_xx_tblDecisao]', N'tblDecisao';

COMMIT TRANSACTION;

SET TRANSACTION ISOLATION LEVEL READ COMMITTED;


GO
PRINT N'Creating [dbo].[FK_tblPra_tblDecisao]...';


GO
ALTER TABLE [dbo].[tblPra] WITH NOCHECK
    ADD CONSTRAINT [FK_tblPra_tblDecisao] FOREIGN KEY ([id_decisao]) REFERENCES [dbo].[tblDecisao] ([id_decisao]);


GO
PRINT N'Checking existing data against newly created constraints';


GO
USE [$(DatabaseName)];


GO
ALTER TABLE [dbo].[tblPra] WITH CHECK CHECK CONSTRAINT [FK_tblPra_tblDecisao];


GO
PRINT N'Update complete.';


GO
