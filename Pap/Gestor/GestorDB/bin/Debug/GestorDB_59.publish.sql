/*
Deployment script for GestorDB

This code was generated by a tool.
Changes to this file may cause incorrect behavior and will be lost if
the code is regenerated.
*/

GO
SET ANSI_NULLS, ANSI_PADDING, ANSI_WARNINGS, ARITHABORT, CONCAT_NULL_YIELDS_NULL, QUOTED_IDENTIFIER ON;

SET NUMERIC_ROUNDABORT OFF;


GO
:setvar DatabaseName "GestorDB"
:setvar DefaultFilePrefix "GestorDB"
:setvar DefaultDataPath "C:\Users\jonip\AppData\Local\Microsoft\Microsoft SQL Server Local DB\Instances\MSSQLLocalDB\"
:setvar DefaultLogPath "C:\Users\jonip\AppData\Local\Microsoft\Microsoft SQL Server Local DB\Instances\MSSQLLocalDB\"

GO
:on error exit
GO
/*
Detect SQLCMD mode and disable script execution if SQLCMD mode is not supported.
To re-enable the script after enabling SQLCMD mode, execute the following:
SET NOEXEC OFF; 
*/
:setvar __IsSqlCmdEnabled "True"
GO
IF N'$(__IsSqlCmdEnabled)' NOT LIKE N'True'
    BEGIN
        PRINT N'SQLCMD mode must be enabled to successfully execute this script.';
        SET NOEXEC ON;
    END


GO
USE [$(DatabaseName)];


GO
/*
The column [dbo].[tblDecisao].[decisao_code] on table [dbo].[tblDecisao] must be added, but the column has no default value and does not allow NULL values. If the table contains data, the ALTER script will not work. To avoid this issue you must either: add a default value to the column, mark it as allowing NULL values, or enable the generation of smart-defaults as a deployment option.
*/

IF EXISTS (select top 1 1 from [dbo].[tblDecisao])
    RAISERROR (N'Rows were detected. The schema update is terminating because data loss might occur.', 16, 127) WITH NOWAIT

GO
PRINT N'Altering [dbo].[tblDecisao]...';


GO
ALTER TABLE [dbo].[tblDecisao]
    ADD [decisao_code] CHAR (61) NOT NULL;


GO
PRINT N'Altering [dbo].[sp_UpdateDecisaoByID]...';


GO
ALTER PROCEDURE [dbo].[sp_UpdateDecisaoByID]
	@id_decisao int,
	@data_conselho date,
	@consequencia varchar(100),
	@data_eadpf date,
	@medidas_c_s varchar(100),
	@assinatura_diretor varchar(50),
	@data_assinatura_diretor date,
	@decisao_code char(61),
	@id_pra int

AS
begin
	declare @count int 
	SELECT @count = COUNT (*) from tblDecisao
	where id_decisao=@id_decisao

	if(@count=0)
		begin
			select -1 as ReturnCode
		End
	Else
	Begin
		update tblDecisao 
		set data_conselho=@data_conselho , consequencia=@consequencia , data_eadpf = @data_eadpf, medidas_c_s = @medidas_c_s, assinatura_diretor= @assinatura_diretor, data_assinatura_diretor = @data_assinatura_diretor,decisao_code=@decisao_code,id_pra=@id_pra
		where id_decisao=@id_decisao
		select 1 as ReturnCode
	END
END
GO
PRINT N'Refreshing [dbo].[sp_InsertDecisao]...';


GO
EXECUTE sp_refreshsqlmodule N'[dbo].[sp_InsertDecisao]';


GO
PRINT N'Update complete.';


GO
