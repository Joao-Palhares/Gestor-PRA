/*
Deployment script for GestorDB

This code was generated by a tool.
Changes to this file may cause incorrect behavior and will be lost if
the code is regenerated.
*/

GO
SET ANSI_NULLS, ANSI_PADDING, ANSI_WARNINGS, ARITHABORT, CONCAT_NULL_YIELDS_NULL, QUOTED_IDENTIFIER ON;

SET NUMERIC_ROUNDABORT OFF;


GO
:setvar DatabaseName "GestorDB"
:setvar DefaultFilePrefix "GestorDB"
:setvar DefaultDataPath "C:\Users\jonip\AppData\Local\Microsoft\Microsoft SQL Server Local DB\Instances\MSSQLLocalDB\"
:setvar DefaultLogPath "C:\Users\jonip\AppData\Local\Microsoft\Microsoft SQL Server Local DB\Instances\MSSQLLocalDB\"

GO
:on error exit
GO
/*
Detect SQLCMD mode and disable script execution if SQLCMD mode is not supported.
To re-enable the script after enabling SQLCMD mode, execute the following:
SET NOEXEC OFF; 
*/
:setvar __IsSqlCmdEnabled "True"
GO
IF N'$(__IsSqlCmdEnabled)' NOT LIKE N'True'
    BEGIN
        PRINT N'SQLCMD mode must be enabled to successfully execute this script.';
        SET NOEXEC ON;
    END


GO
USE [$(DatabaseName)];


GO
/*
The column [dbo].[tblFaltas].[dia_da_falta] is being dropped, data loss could occur.

The column [dbo].[tblFaltas].[id_modulo] is being dropped, data loss could occur.

The column [dbo].[tblFaltas].[tempos_lecionados] is being dropped, data loss could occur.

The column [dbo].[tblFaltas].[id_aluno] on table [dbo].[tblFaltas] must be added, but the column has no default value and does not allow NULL values. If the table contains data, the ALTER script will not work. To avoid this issue you must either: add a default value to the column, mark it as allowing NULL values, or enable the generation of smart-defaults as a deployment option.

The column [dbo].[tblFaltas].[id_turma] on table [dbo].[tblFaltas] must be added, but the column has no default value and does not allow NULL values. If the table contains data, the ALTER script will not work. To avoid this issue you must either: add a default value to the column, mark it as allowing NULL values, or enable the generation of smart-defaults as a deployment option.

The column [dbo].[tblFaltas].[injustificada_justificada] on table [dbo].[tblFaltas] must be added, but the column has no default value and does not allow NULL values. If the table contains data, the ALTER script will not work. To avoid this issue you must either: add a default value to the column, mark it as allowing NULL values, or enable the generation of smart-defaults as a deployment option.

The column [dbo].[tblFaltas].[tempo_letivo] on table [dbo].[tblFaltas] must be added, but the column has no default value and does not allow NULL values. If the table contains data, the ALTER script will not work. To avoid this issue you must either: add a default value to the column, mark it as allowing NULL values, or enable the generation of smart-defaults as a deployment option.
*/

IF EXISTS (select top 1 1 from [dbo].[tblFaltas])
    RAISERROR (N'Rows were detected. The schema update is terminating because data loss might occur.', 16, 127) WITH NOWAIT

GO
PRINT N'Rename refactoring operation with key 739f7ff7-5c6b-478f-a72a-39240bedb6f7 is skipped, element [dbo].[tblFaltas].[Tempo] (SqlSimpleColumn) will not be renamed to tempo_letivo';


GO
PRINT N'Rename refactoring operation with key ea18e7d7-09b5-4262-87a6-ed47c6bd9169 is skipped, element [dbo].[tblFaltas].[justificação] (SqlSimpleColumn) will not be renamed to injustificada_justificada';


GO
PRINT N'Starting rebuilding table [dbo].[tblFaltas]...';


GO
BEGIN TRANSACTION;

SET TRANSACTION ISOLATION LEVEL SERIALIZABLE;

SET XACT_ABORT ON;

CREATE TABLE [dbo].[tmp_ms_xx_tblFaltas] (
    [id_falta]                  INT           IDENTITY (1, 1) NOT NULL,
    [id_turma]                  INT           NOT NULL,
    [id_disciplina]             INT           NOT NULL,
    [id_aluno]                  INT           NOT NULL,
    [tempo_letivo]              INT           NOT NULL,
    [injustificada_justificada] VARCHAR (13)  NOT NULL,
    [justificacao]              VARCHAR (100) NULL,
    PRIMARY KEY CLUSTERED ([id_falta] ASC)
);

IF EXISTS (SELECT TOP 1 1 
           FROM   [dbo].[tblFaltas])
    BEGIN
        SET IDENTITY_INSERT [dbo].[tmp_ms_xx_tblFaltas] ON;
        INSERT INTO [dbo].[tmp_ms_xx_tblFaltas] ([id_falta], [id_disciplina])
        SELECT   [id_falta],
                 [id_disciplina]
        FROM     [dbo].[tblFaltas]
        ORDER BY [id_falta] ASC;
        SET IDENTITY_INSERT [dbo].[tmp_ms_xx_tblFaltas] OFF;
    END

DROP TABLE [dbo].[tblFaltas];

EXECUTE sp_rename N'[dbo].[tmp_ms_xx_tblFaltas]', N'tblFaltas';

COMMIT TRANSACTION;

SET TRANSACTION ISOLATION LEVEL READ COMMITTED;


GO
PRINT N'Altering [dbo].[sp_GetPrhs]...';


GO
ALTER PROCEDURE [dbo].[sp_GetPrhs]
AS
BEGIN
	SELECT tblPrh.id_prh ,tblAlunos.nome, tblTurmas.nome_turma, tblProfessores.nome , tblDT.nome
FROM tblPrh
INNER JOIN tblAlunos ON tblPrh.id_aluno = tblAlunos.id_aluno
INNER JOIN tblProfessores ON tblPrh.id_professor = tblProfessores.id_professor
INNER JOIN tblTurmas ON tblPrh.id_turma = tblTurmas.id_turma 
INNER JOIN tblDT ON tblPrh.id_dt = tblDT.id_dt
END
GO
PRINT N'Creating [dbo].[sp_GetDTByProfessor]...';


GO
CREATE PROCEDURE [dbo].[sp_GetDTByProfessor]
	@id_professor int

AS
begin
		select * From tblDT where id_professor=@id_professor
END
GO
-- Refactoring step to update target server with deployed transaction logs
IF NOT EXISTS (SELECT OperationKey FROM [dbo].[__RefactorLog] WHERE OperationKey = '739f7ff7-5c6b-478f-a72a-39240bedb6f7')
INSERT INTO [dbo].[__RefactorLog] (OperationKey) values ('739f7ff7-5c6b-478f-a72a-39240bedb6f7')
IF NOT EXISTS (SELECT OperationKey FROM [dbo].[__RefactorLog] WHERE OperationKey = 'ea18e7d7-09b5-4262-87a6-ed47c6bd9169')
INSERT INTO [dbo].[__RefactorLog] (OperationKey) values ('ea18e7d7-09b5-4262-87a6-ed47c6bd9169')

GO

GO
PRINT N'Update complete.';


GO
