/*
Deployment script for GestorDB

This code was generated by a tool.
Changes to this file may cause incorrect behavior and will be lost if
the code is regenerated.
*/

GO
SET ANSI_NULLS, ANSI_PADDING, ANSI_WARNINGS, ARITHABORT, CONCAT_NULL_YIELDS_NULL, QUOTED_IDENTIFIER ON;

SET NUMERIC_ROUNDABORT OFF;


GO
:setvar DatabaseName "GestorDB"
:setvar DefaultFilePrefix "GestorDB"
:setvar DefaultDataPath "C:\Users\jonip\AppData\Local\Microsoft\Microsoft SQL Server Local DB\Instances\MSSQLLocalDB\"
:setvar DefaultLogPath "C:\Users\jonip\AppData\Local\Microsoft\Microsoft SQL Server Local DB\Instances\MSSQLLocalDB\"

GO
:on error exit
GO
/*
Detect SQLCMD mode and disable script execution if SQLCMD mode is not supported.
To re-enable the script after enabling SQLCMD mode, execute the following:
SET NOEXEC OFF; 
*/
:setvar __IsSqlCmdEnabled "True"
GO
IF N'$(__IsSqlCmdEnabled)' NOT LIKE N'True'
    BEGIN
        PRINT N'SQLCMD mode must be enabled to successfully execute this script.';
        SET NOEXEC ON;
    END


GO
USE [$(DatabaseName)];


GO
PRINT N'Creating [dbo].[sp_UpdateDMByID1]...';


GO
CREATE PROCEDURE [dbo].[sp_UpdateDMByID1]
	@id_dm int,
	@disciplina_modulo varchar(50),
	@faltas_excesso int,
	@assinatura_professor_disciplina varchar(50),
	@data_assinatura date,
	@avaliaçao varchar(50),
	@retido varchar(50),
	@id_pra int,
	@dmcode char(50)

AS
begin
	declare @count int 
	SELECT @count = COUNT (*) from tblPraDM
	where id_dm=@id_dm

	if(@count=0)
		begin
			select -1 as ReturnCode
		End
	Else
	Begin
		update tblPraDM 
		set id_dm=@id_dm , disciplina_modulo=@disciplina_modulo , faltas_excesso = @faltas_excesso, assinatura_professor_disciplina = @assinatura_professor_disciplina, data_assinatura= @data_assinatura, avaliaçao = @avaliaçao, retido=@retido, id_pra=@id_pra, dmcode=@dmcode
		where id_dm=@id_dm
		select 1 as ReturnCode
	END
END
GO
PRINT N'Creating [dbo].[sp_UpdateDMByID2]...';


GO
CREATE PROCEDURE [dbo].[sp_UpdateDMByID2]
	@id_dm int,
	@disciplina_modulo varchar(50),
	@faltas_excesso int,
	@assinatura_professor_disciplina varchar(50),
	@data_assinatura date,
	@avaliaçao varchar(50),
	@retido varchar(50),
	@id_pra int,
	@dmcode char(50)

AS
begin
	declare @count int 
	SELECT @count = COUNT (*) from tblPraDM
	where id_dm=@id_dm

	if(@count=0)
		begin
			select -1 as ReturnCode
		End
	Else
	Begin
		update tblPraDM 
		set id_dm=@id_dm , disciplina_modulo=@disciplina_modulo , faltas_excesso = @faltas_excesso, assinatura_professor_disciplina = @assinatura_professor_disciplina, data_assinatura= @data_assinatura, avaliaçao = @avaliaçao, retido=@retido, id_pra=@id_pra, dmcode=@dmcode
		where id_dm=@id_dm
		select 1 as ReturnCode
	END
END
GO
PRINT N'Creating [dbo].[sp_UpdateDMByID3]...';


GO
CREATE PROCEDURE [dbo].[sp_UpdateDMByID3]
	@id_dm int,
	@disciplina_modulo varchar(50),
	@faltas_excesso int,
	@assinatura_professor_disciplina varchar(50),
	@data_assinatura date,
	@avaliaçao varchar(50),
	@retido varchar(50),
	@id_pra int,
	@dmcode char(50)

AS
begin
	declare @count int 
	SELECT @count = COUNT (*) from tblPraDM
	where id_dm=@id_dm

	if(@count=0)
		begin
			select -1 as ReturnCode
		End
	Else
	Begin
		update tblPraDM 
		set id_dm=@id_dm , disciplina_modulo=@disciplina_modulo , faltas_excesso = @faltas_excesso, assinatura_professor_disciplina = @assinatura_professor_disciplina, data_assinatura= @data_assinatura, avaliaçao = @avaliaçao, retido=@retido, id_pra=@id_pra, dmcode=@dmcode
		where id_dm=@id_dm
		select 1 as ReturnCode
	END
END
GO
PRINT N'Creating [dbo].[sp_UpdateDMByID4]...';


GO
CREATE PROCEDURE [dbo].[sp_UpdateDMByID4]
	@id_dm int,
	@disciplina_modulo varchar(50),
	@faltas_excesso int,
	@assinatura_professor_disciplina varchar(50),
	@data_assinatura date,
	@avaliaçao varchar(50),
	@retido varchar(50),
	@id_pra int,
	@dmcode char(50)

AS
begin
	declare @count int 
	SELECT @count = COUNT (*) from tblPraDM
	where id_dm=@id_dm

	if(@count=0)
		begin
			select -1 as ReturnCode
		End
	Else
	Begin
		update tblPraDM 
		set id_dm=@id_dm , disciplina_modulo=@disciplina_modulo , faltas_excesso = @faltas_excesso, assinatura_professor_disciplina = @assinatura_professor_disciplina, data_assinatura= @data_assinatura, avaliaçao = @avaliaçao, retido=@retido, id_pra=@id_pra, dmcode=@dmcode
		where id_dm=@id_dm
		select 1 as ReturnCode
	END
END
GO
PRINT N'Creating [dbo].[sp_UpdateDMByID5]...';


GO
CREATE PROCEDURE [dbo].[sp_UpdateDMByID5]
	@id_dm int,
	@disciplina_modulo varchar(50),
	@faltas_excesso int,
	@assinatura_professor_disciplina varchar(50),
	@data_assinatura date,
	@avaliaçao varchar(50),
	@retido varchar(50),
	@id_pra int,
	@dmcode char(50)

AS
begin
	declare @count int 
	SELECT @count = COUNT (*) from tblPraDM
	where id_dm=@id_dm

	if(@count=0)
		begin
			select -1 as ReturnCode
		End
	Else
	Begin
		update tblPraDM 
		set id_dm=@id_dm , disciplina_modulo=@disciplina_modulo , faltas_excesso = @faltas_excesso, assinatura_professor_disciplina = @assinatura_professor_disciplina, data_assinatura= @data_assinatura, avaliaçao = @avaliaçao, retido=@retido, id_pra=@id_pra, dmcode=@dmcode
		where id_dm=@id_dm
		select 1 as ReturnCode
	END
END
GO
PRINT N'Update complete.';


GO
