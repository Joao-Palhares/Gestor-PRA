/*
Deployment script for GestorDB

This code was generated by a tool.
Changes to this file may cause incorrect behavior and will be lost if
the code is regenerated.
*/

GO
SET ANSI_NULLS, ANSI_PADDING, ANSI_WARNINGS, ARITHABORT, CONCAT_NULL_YIELDS_NULL, QUOTED_IDENTIFIER ON;

SET NUMERIC_ROUNDABORT OFF;


GO
:setvar DatabaseName "GestorDB"
:setvar DefaultFilePrefix "GestorDB"
:setvar DefaultDataPath "C:\Users\jonip\AppData\Local\Microsoft\Microsoft SQL Server Local DB\Instances\MSSQLLocalDB\"
:setvar DefaultLogPath "C:\Users\jonip\AppData\Local\Microsoft\Microsoft SQL Server Local DB\Instances\MSSQLLocalDB\"

GO
:on error exit
GO
/*
Detect SQLCMD mode and disable script execution if SQLCMD mode is not supported.
To re-enable the script after enabling SQLCMD mode, execute the following:
SET NOEXEC OFF; 
*/
:setvar __IsSqlCmdEnabled "True"
GO
IF N'$(__IsSqlCmdEnabled)' NOT LIKE N'True'
    BEGIN
        PRINT N'SQLCMD mode must be enabled to successfully execute this script.';
        SET NOEXEC ON;
    END


GO
USE [$(DatabaseName)];


GO
/*
The column [dbo].[tblPra].[codigo_pra] on table [dbo].[tblPra] must be added, but the column has no default value and does not allow NULL values. If the table contains data, the ALTER script will not work. To avoid this issue you must either: add a default value to the column, mark it as allowing NULL values, or enable the generation of smart-defaults as a deployment option.
*/

IF EXISTS (select top 1 1 from [dbo].[tblPra])
    RAISERROR (N'Rows were detected. The schema update is terminating because data loss might occur.', 16, 127) WITH NOWAIT

GO
/*
The column [dbo].[tblPrh].[codigo_prh] on table [dbo].[tblPrh] must be added, but the column has no default value and does not allow NULL values. If the table contains data, the ALTER script will not work. To avoid this issue you must either: add a default value to the column, mark it as allowing NULL values, or enable the generation of smart-defaults as a deployment option.
*/

IF EXISTS (select top 1 1 from [dbo].[tblPrh])
    RAISERROR (N'Rows were detected. The schema update is terminating because data loss might occur.', 16, 127) WITH NOWAIT

GO
PRINT N'Altering [dbo].[tblPra]...';


GO
ALTER TABLE [dbo].[tblPra]
    ADD [codigo_pra] VARCHAR (20) NOT NULL;


GO
PRINT N'Altering [dbo].[tblPrh]...';


GO
ALTER TABLE [dbo].[tblPrh]
    ADD [codigo_prh] VARCHAR (20) NOT NULL;


GO
PRINT N'Creating [dbo].[sp_GetPraByCode]...';


GO
CREATE PROCEDURE [dbo].[sp_GetPraByCode]
	@codigo_pra varchar(20)
AS
begin
		select * From tblPra where codigo_pra=@codigo_pra
END
GO
PRINT N'Creating [dbo].[sp_InsertAvaliacao]...';


GO
CREATE PROCEDURE [dbo].[sp_InsertAvaliacao]
	@id_avaliaçoes int,
	@avaliaçao_atividade varchar(1000),
	@faltas_desconsideradas varchar(1000),
	@nome_aluno varchar(200),
	@data_assinatura_aluno date,
	@nome_professor varchar(200),
	@data_assinatura_professor date,
	@dt_assinatura varchar(200),
	@data_assinatura_dt date

AS
Begin
	Declare @count int
	SELECT @count= COUNT(id_avaliaçoes) FROM tblAvaliacao Where id_avaliaçoes=@id_avaliaçoes

	IF(@count<>0)
	begin 
		select -1 as ReturnCode3
	End
	Begin
		Insert INTO [dbo].[tblAvaliacao]
				([id_avaliaçoes],
				[avaliaçao_atividade],
				[faltas_desconsideradas],
				[nome_aluno],
				[data_assinatura_aluno],
				[nome_professor],
				[data_assinatura_professor],
				[dt_assinatura],
				[data_assinatura_dt])
		Values (@id_avaliaçoes,@avaliaçao_atividade,@faltas_desconsideradas,@nome_aluno,@data_assinatura_aluno,@nome_professor,@data_assinatura_professor,@dt_assinatura,@data_assinatura_dt)
		select 1 as ReturnCode3
	END
END
GO
PRINT N'Creating [dbo].[sp_InsertDecisao]...';


GO
CREATE PROCEDURE [dbo].[sp_InsertDecisao]
	@id_decisao int,
	@data_conselho date,
	@data_eadpf date,
	@medidas_c_S varchar(100),
	@consequencia varchar(100),
	@assinatura_diretor varchar(50),
	@data_assinatura_diretor date

AS
Begin
	Declare @count int
	SELECT @count= COUNT(id_decisao) FROM tblDecisao Where id_decisao=@id_decisao

	IF(@count<>0)
	begin 
		select -1 as ReturnCode1
	End
	Begin
		Insert INTO [dbo].[tblDecisao]
				([id_decisao],
				[data_eadpf],
				[medidas_c_s],
				[data_conselho],
				[consequencia],
				[assinatura_diretor],
				[data_assinatura_diretor])
		Values (@id_decisao,@data_eadpf,@medidas_c_S,@data_conselho,@consequencia,@assinatura_diretor,@data_assinatura_diretor)
		select 1 as ReturnCode1
	END
END
GO
PRINT N'Creating [dbo].[sp_InsertDescricaoatividades]...';


GO
CREATE PROCEDURE [dbo].[sp_InsertDescricaoatividades]
	@id_descriçao_atividade int,
	@atividades varchar(300),
	@local varchar(100),
	@data_inicio date,
	@data_final date,
	@horario_inicial time,
	@horario_final time,
	@id_prh int

AS
Begin
	Declare @count int
	SELECT @count= COUNT(id_descriçao_atividade) FROM tbldescricaoatividades Where id_descriçao_atividade=@id_descriçao_atividade

	IF(@count<>0)
	begin 
		select -1 as ReturnCode21
	End
	Begin
		Insert INTO [dbo].[tbldescricaoatividades]
				([id_descriçao_atividade],
				[atividades],
				[local],
				[data_inicio],
				[data_final],
				[horario_inicial],
				[horario_final],
				[id_prh])
		Values (@id_descriçao_atividade,@atividades,@local,@data_inicio,@data_final,@horario_inicial,@horario_final,@id_prh)
		select 1 as ReturnCode21
	END
END
GO
PRINT N'Creating [dbo].[sp_InsertDescricaoatividades2]...';


GO
CREATE PROCEDURE [dbo].[sp_InsertDescricaoatividades2]
	@id_descriçao_atividade int,
	@atividades varchar(300),
	@local varchar(100),
	@data_inicio date,
	@data_final date,
	@horario_inicial time,
	@horario_final time,
	@id_prh int

AS
Begin
	Declare @count int
	SELECT @count= COUNT(id_descriçao_atividade) FROM tbldescricaoatividades Where id_descriçao_atividade=@id_descriçao_atividade

	IF(@count<>0)
	begin 
		select -1 as ReturnCode22
	End
	Begin
		Insert INTO [dbo].[tbldescricaoatividades]
				([id_descriçao_atividade],
				[atividades],
				[local],
				[data_inicio],
				[data_final],
				[horario_inicial],
				[horario_final],
				[id_prh])
		Values (@id_descriçao_atividade,@atividades,@local,@data_inicio,@data_final,@horario_inicial,@horario_final,@id_prh)
		select 1 as ReturnCode22
	END
END
GO
PRINT N'Creating [dbo].[sp_InsertDescricaoatividades3]...';


GO
CREATE PROCEDURE [dbo].[sp_InsertDescricaoatividades3]
	@id_descriçao_atividade int,
	@atividades varchar(300),
	@local varchar(100),
	@data_inicio date,
	@data_final date,
	@horario_inicial time,
	@horario_final time,
	@id_prh int

AS
Begin
	Declare @count int
	SELECT @count= COUNT(id_descriçao_atividade) FROM tbldescricaoatividades Where id_descriçao_atividade=@id_descriçao_atividade

	IF(@count<>0)
	begin 
		select -1 as ReturnCode23
	End
	Begin
		Insert INTO [dbo].[tbldescricaoatividades]
				([id_descriçao_atividade],
				[atividades],
				[local],
				[data_inicio],
				[data_final],
				[horario_inicial],
				[horario_final],
				[id_prh])
		Values (@id_descriçao_atividade,@atividades,@local,@data_inicio,@data_final,@horario_inicial,@horario_final,@id_prh)
		select 1 as ReturnCode23
	END
END
GO
PRINT N'Creating [dbo].[sp_InsertDescricaoatividades4]...';


GO
CREATE PROCEDURE [dbo].[sp_InsertDescricaoatividades4]
	@id_descriçao_atividade int,
	@atividades varchar(300),
	@local varchar(100),
	@data_inicio date,
	@data_final date,
	@horario_inicial time,
	@horario_final time,
	@id_prh int

AS
Begin
	Declare @count int
	SELECT @count= COUNT(id_descriçao_atividade) FROM tbldescricaoatividades Where id_descriçao_atividade=@id_descriçao_atividade

	IF(@count<>0)
	begin 
		select -1 as ReturnCode24
	End
	Begin
		Insert INTO [dbo].[tbldescricaoatividades]
				([id_descriçao_atividade],
				[atividades],
				[local],
				[data_inicio],
				[data_final],
				[horario_inicial],
				[horario_final],
				[id_prh])
		Values (@id_descriçao_atividade,@atividades,@local,@data_inicio,@data_final,@horario_inicial,@horario_final,@id_prh)
		select 1 as ReturnCode24
	END
END
GO
PRINT N'Creating [dbo].[sp_InsertDescricaoatividades5]...';


GO
CREATE PROCEDURE [dbo].[sp_InsertDescricaoatividades5]
	@id_descriçao_atividade int,
	@atividades varchar(300),
	@local varchar(100),
	@data_inicio date,
	@data_final date,
	@horario_inicial time,
	@horario_final time,
	@id_prh int

AS
Begin
	Declare @count int
	SELECT @count= COUNT(id_descriçao_atividade) FROM tbldescricaoatividades Where id_descriçao_atividade=@id_descriçao_atividade

	IF(@count<>0)
	begin 
		select -1 as ReturnCode25
	End
	Begin
		Insert INTO [dbo].[tbldescricaoatividades]
				([id_descriçao_atividade],
				[atividades],
				[local],
				[data_inicio],
				[data_final],
				[horario_inicial],
				[horario_final],
				[id_prh])
		Values (@id_descriçao_atividade,@atividades,@local,@data_inicio,@data_final,@horario_inicial,@horario_final,@id_prh)
		select 1 as ReturnCode25
	END
END
GO
PRINT N'Creating [dbo].[sp_InsertDM]...';


GO
CREATE PROCEDURE [dbo].[sp_InsertDM]
	@id_dm int,
	@disciplina_modulo varchar(50),
	@faltas_excesso int,
	@assinatura_professor_disciplina varchar(50),
	@data_assinatura date,
	@avaliaçao varchar(50),
	@retido varchar(50)

AS
Begin
	Declare @count int
	SELECT @count= COUNT(id_dm) FROM tblPraDM Where id_dm=@id_dm

	IF(@count<>0)
	begin 
		select -1 as ReturnCode2
	End
	Begin
		Insert INTO [dbo].[tblPraDM]
				([id_dm],
				[disciplina_modulo],
				[faltas_excesso],
				[assinatura_professor_disciplina],
				[data_assinatura],
				[cumprimento],
				[retido])
		Values (@id_dm,@disciplina_modulo,@faltas_excesso,@assinatura_professor_disciplina,@data_assinatura,@avaliaçao,@retido)
		select 1 as ReturnCode2
	END
END
GO
PRINT N'Creating [dbo].[sp_InsertDM2]...';


GO
CREATE PROCEDURE [dbo].[sp_InsertDM2]
	@id_dm int,
	@disciplina_modulo varchar(50),
	@faltas_excesso int,
	@assinatura_professor_disciplina varchar(50),
	@data_assinatura date,
	@avaliaçao varchar(50),
	@retido varchar(50)

AS
Begin
	Declare @count int
	SELECT @count= COUNT(id_dm) FROM tblPraDM Where id_dm=@id_dm

	IF(@count<>0)
	begin 
		select -1 as ReturnCode2
	End
	Begin
		Insert INTO [dbo].[tblPraDM]
				([id_dm],
				[disciplina_modulo],
				[faltas_excesso],
				[assinatura_professor_disciplina],
				[data_assinatura],
				[cumprimento],
				[retido])
		Values (@id_dm,@disciplina_modulo,@faltas_excesso,@assinatura_professor_disciplina,@data_assinatura,@avaliaçao,@retido)
		select 1 as ReturnCode22
	END
END
GO
PRINT N'Creating [dbo].[sp_InsertDM3]...';


GO
CREATE PROCEDURE [dbo].[sp_InsertDM3]
	@id_dm int,
	@disciplina_modulo varchar(50),
	@faltas_excesso int,
	@assinatura_professor_disciplina varchar(50),
	@data_assinatura date,
	@avaliaçao varchar(50),
	@retido varchar(50)

AS
Begin
	Declare @count int
	SELECT @count= COUNT(id_dm) FROM tblPraDM Where id_dm=@id_dm

	IF(@count<>0)
	begin 
		select -1 as ReturnCode2
	End
	Begin
		Insert INTO [dbo].[tblPraDM]
				([id_dm],
				[disciplina_modulo],
				[faltas_excesso],
				[assinatura_professor_disciplina],
				[data_assinatura],
				[cumprimento],
				[retido])
		Values (@id_dm,@disciplina_modulo,@faltas_excesso,@assinatura_professor_disciplina,@data_assinatura,@avaliaçao,@retido)
		select 1 as ReturnCode23
	END
END
GO
PRINT N'Creating [dbo].[sp_InsertDM4]...';


GO
CREATE PROCEDURE [dbo].[sp_InsertDM4]
	@id_dm int,
	@disciplina_modulo varchar(50),
	@faltas_excesso int,
	@assinatura_professor_disciplina varchar(50),
	@data_assinatura date,
	@avaliaçao varchar(50),
	@retido varchar(50)

AS
Begin
	Declare @count int
	SELECT @count= COUNT(id_dm) FROM tblPraDM Where id_dm=@id_dm

	IF(@count<>0)
	begin 
		select -1 as ReturnCode2
	End
	Begin
		Insert INTO [dbo].[tblPraDM]
				([id_dm],
				[disciplina_modulo],
				[faltas_excesso],
				[assinatura_professor_disciplina],
				[data_assinatura],
				[cumprimento],
				[retido])
		Values (@id_dm,@disciplina_modulo,@faltas_excesso,@assinatura_professor_disciplina,@data_assinatura,@avaliaçao,@retido)
		select 1 as ReturnCode24
	END
END
GO
PRINT N'Creating [dbo].[sp_InsertDM5]...';


GO
CREATE PROCEDURE [dbo].[sp_InsertDM5]
	@id_dm int,
	@disciplina_modulo varchar(50),
	@faltas_excesso int,
	@assinatura_professor_disciplina varchar(50),
	@data_assinatura date,
	@avaliaçao varchar(50),
	@retido varchar(50)

AS
Begin
	Declare @count int
	SELECT @count= COUNT(id_dm) FROM tblPraDM Where id_dm=@id_dm

	IF(@count<>0)
	begin 
		select -1 as ReturnCode2
	End
	Begin
		Insert INTO [dbo].[tblPraDM]
				([id_dm],
				[disciplina_modulo],
				[faltas_excesso],
				[assinatura_professor_disciplina],
				[data_assinatura],
				[cumprimento],
				[retido])
		Values (@id_dm,@disciplina_modulo,@faltas_excesso,@assinatura_professor_disciplina,@data_assinatura,@avaliaçao,@retido)
		select 1 as ReturnCode25
	END
END
GO
PRINT N'Creating [dbo].[sp_InsertMedidas]...';


GO
CREATE PROCEDURE [dbo].[sp_InsertMedidas]
	@id_medidas int,
	@periodo_inicio datetime,
	@periodo_fim datetime,
	@medida varchar(300),
	@cumprimento varchar(50),
	@data_cumprimento date,
	@dever_incumprimento varchar(50),
	@data_incumprimento date,
	@faltas_desconsideradas varchar(300)

AS
Begin
	Declare @count int
	SELECT @count= COUNT(id_medidas) FROM tblPraMedidas Where id_medidas=@id_medidas

	IF(@count<>0)
	begin 
		select -1 as ReturnCode4
	End
	Begin
		Insert INTO [dbo].[tblPraMedidas]
				([id_medidas],
				[periodo_inicio],
				[periodo_fim],
				[medida],
				[cumprimento],
				[data_cumprimento],
				[data_incumprimento],
				[dever_incumprimento],
				[data_incumprimento],
				[faltas_desconsideradas])
		Values (@id_medidas,@periodo_inicio,@periodo_fim,@medida,@cumprimento,@data_cumprimento,@dever_incumprimento,@data_incumprimento,@faltas_desconsideradas)
		select 1 as ReturnCode4
	END
END
GO
PRINT N'Creating [dbo].[sp_InsertNotificacoes]...';


GO
CREATE PROCEDURE [dbo].[sp_InsertNotificacoes]
	@id_notificaçoes int,
	@assinatura_enc varchar(50),
	@data_assinatura_enc date,
	@assinatura_dt varchar(50),
	@data_assinatura_dt date,
	@assinatura_pt varchar(50),
	@data_assinatura_pt date,
	@data_assinatura_cpcj date

AS
Begin
	Declare @count int
	SELECT @count= COUNT(id_notificaçoes) FROM tblPraNotificacoes Where id_notificaçoes=@id_notificaçoes

	IF(@count<>0)
	begin 
		select -1 as ReturnCode4
	End
	Begin
		Insert INTO [dbo].[tblPraNotificacoes]
				([id_notificaçoes],
				[assinatura_enc],
				[data_assinatura_enc],
				[assinatura_dt],
				[data_assinatura_dt],
				[assinatura_pt],
				[data_assinatura_pt],
				[data_assinatura_cpcj])
		Values (@id_notificaçoes,@assinatura_enc,@data_assinatura_enc,@assinatura_dt,@data_assinatura_dt,@assinatura_pt,@data_assinatura_pt,@data_assinatura_cpcj)
		select 1 as ReturnCode4
	END
END
GO
PRINT N'Creating [dbo].[sp_InsertPra]...';


GO
CREATE PROCEDURE [dbo].[sp_InsertPra]
	@id_pra int,
	@id_principal int,
	@id_dm int,
	@id_medidas int,
	@id_notificaçoes int,
	@id_decisao int,
	@codigo_pra varchar(20)

AS
Begin
	Declare @count int
	SELECT @count= COUNT(id_pra) FROM tblPra Where id_pra=@id_pra

	IF(@count<>0)
	begin 
		select -1 as ReturnCodepra
	End
	Begin
		Insert INTO [dbo].[tblPra]
				([id_pra],
				[id_principal],
				[id_dm],
				[id_medidas],
				[id_notificaçoes],
				[id_decisao],
				[codigo_pra])
		Values (@id_pra,@id_principal,@id_dm,@id_medidas,@id_notificaçoes,@id_decisao,@codigo_pra)
		select 1 as ReturnCodepra
	END
END
GO
PRINT N'Creating [dbo].[sp_InsertPrh]...';


GO
CREATE PROCEDURE [dbo].[sp_InsertPrh]
	@id_prh int,
	@id_principal int,
	@id_avaliaçoes int,
	@codigo_prh varchar(20)

AS
Begin
	Declare @count int
	SELECT @count= COUNT(id_prh) FROM tblPrh Where id_prh=@id_prh

	IF(@count<>0)
	begin 
		select -1 as ReturnCode
	End
	Begin
		Insert INTO [dbo].[tblPrh]
				([id_prh],
				[id_principal],
				[id_avaliaçoes])
		Values (@id_prh,@id_principal,@id_avaliaçoes)
		select 1 as ReturnCode
	END
END
GO
PRINT N'Creating [dbo].[sp_InsertPrhPrincipal]...';


GO
CREATE PROCEDURE [dbo].[sp_InsertPrhPrincipal]
	@id_principal int,
	@ano_letivo char(13),
	@turma varchar(100),
	@numero_aluno int,
	@nome_aluno varchar(200),
	@curso varchar(200),
	@disciplina varchar(50),
	@tempo_letivos_faltas int,
	@modalidade_adotada varchar(400),
	@outra_modalidade varchar(100)

AS
Begin
	Declare @count int
	SELECT @count= COUNT(id_principal) FROM tblPrhPrincipal Where id_principal=id_principal

	IF(@count<>0)
	begin 
		select -1 as ReturnCode1
	End
	Begin
		Insert INTO [dbo].[tblPrhPrincipal]
				([id_principal],
				[ano_letivo],
				[turma],
				[numero_aluno],
				[nome_aluno],
				[curso],
				[disciplina],
				[tempo_letivos_faltas],
				[modalidade_adotada],
				[outra_modalidade])
		Values (@id_principal,@ano_letivo,@turma,@numero_aluno,@nome_aluno,@curso,@disciplina,@tempo_letivos_faltas,@modalidade_adotada,@outra_modalidade)
		select 1 as ReturnCode1
	END
END
GO
PRINT N'Creating [dbo].[sp_InsertPrincipal]...';


GO
CREATE PROCEDURE [dbo].[sp_InsertPrincipal]
	@id_principal int,
	@nome_aluno varchar(200),
	@idade int,
	@ano_letivo char(13),
	@turma varchar(100),
	@numero_aluno int

AS
Begin
	Declare @count int
	SELECT @count= COUNT(id_principal) FROM tblPraPrincipal Where id_principal=@id_principal

	IF(@count<>0)
	begin 
		select -1 as ReturnCode
	End
	Begin
		Insert INTO [dbo].[tblPraPrincipal]
				([id_principal],
				[nome_aluno],
				[idade],
				[ano_letivo],
				[turma],
				[numero_aluno])
		Values (@id_principal,@nome_aluno,@idade,@ano_letivo,@turma,@numero_aluno)
		select 1 as ReturnCode
	END
END
GO
PRINT N'Update complete.';


GO
