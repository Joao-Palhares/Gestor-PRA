/*
Deployment script for GestorDB

This code was generated by a tool.
Changes to this file may cause incorrect behavior and will be lost if
the code is regenerated.
*/

GO
SET ANSI_NULLS, ANSI_PADDING, ANSI_WARNINGS, ARITHABORT, CONCAT_NULL_YIELDS_NULL, QUOTED_IDENTIFIER ON;

SET NUMERIC_ROUNDABORT OFF;


GO
:setvar DatabaseName "GestorDB"
:setvar DefaultFilePrefix "GestorDB"
:setvar DefaultDataPath "C:\Users\jonip\AppData\Local\Microsoft\Microsoft SQL Server Local DB\Instances\MSSQLLocalDB\"
:setvar DefaultLogPath "C:\Users\jonip\AppData\Local\Microsoft\Microsoft SQL Server Local DB\Instances\MSSQLLocalDB\"

GO
:on error exit
GO
/*
Detect SQLCMD mode and disable script execution if SQLCMD mode is not supported.
To re-enable the script after enabling SQLCMD mode, execute the following:
SET NOEXEC OFF; 
*/
:setvar __IsSqlCmdEnabled "True"
GO
IF N'$(__IsSqlCmdEnabled)' NOT LIKE N'True'
    BEGIN
        PRINT N'SQLCMD mode must be enabled to successfully execute this script.';
        SET NOEXEC ON;
    END


GO
USE [$(DatabaseName)];


GO
/*
The column [dbo].[tblPraDM].[dmcode] on table [dbo].[tblPraDM] must be added, but the column has no default value and does not allow NULL values. If the table contains data, the ALTER script will not work. To avoid this issue you must either: add a default value to the column, mark it as allowing NULL values, or enable the generation of smart-defaults as a deployment option.
*/

IF EXISTS (select top 1 1 from [dbo].[tblPraDM])
    RAISERROR (N'Rows were detected. The schema update is terminating because data loss might occur.', 16, 127) WITH NOWAIT

GO
PRINT N'Altering [dbo].[tblPraDM]...';


GO
ALTER TABLE [dbo].[tblPraDM]
    ADD [dmcode] CHAR (50) NOT NULL;


GO
PRINT N'Altering [dbo].[sp_InsertDM]...';


GO
ALTER PROCEDURE [dbo].[sp_InsertDM]
	@id_dm int,
	@disciplina_modulo varchar(50),
	@faltas_excesso int,
	@assinatura_professor_disciplina varchar(50),
	@data_assinatura date,
	@avaliaçao varchar(50),
	@retido varchar(50),
	@id_pra int,
	@dmcode char(50)

AS
Begin
	Declare @count int
	SELECT @count= COUNT(id_dm) FROM tblPraDM Where id_dm=@id_dm

	IF(@count<>0)
	begin 
		select -1 as ReturnCode2
	End
	Begin
		Insert INTO [dbo].[tblPraDM]
				([id_dm],
				[disciplina_modulo],
				[faltas_excesso],
				[assinatura_professor_disciplina],
				[data_assinatura],
				[cumprimento],
				[retido],
				[id_pra],
				[dmcode])
		Values (@id_dm,@disciplina_modulo,@faltas_excesso,@assinatura_professor_disciplina,@data_assinatura,@avaliaçao,@retido,@id_pra,@dmcode)
		select 1 as ReturnCode2
	END
END
GO
PRINT N'Altering [dbo].[sp_InsertDM2]...';


GO
ALTER PROCEDURE [dbo].[sp_InsertDM2]
	@id_dm int,
	@disciplina_modulo varchar(50),
	@faltas_excesso int,
	@assinatura_professor_disciplina varchar(50),
	@data_assinatura date,
	@avaliaçao varchar(50),
	@retido varchar(50),
	@id_pra int,
	@dmcode char(50)

AS
Begin
	Declare @count int
	SELECT @count= COUNT(id_dm) FROM tblPraDM Where id_dm=@id_dm

	IF(@count<>0)
	begin 
		select -1 as ReturnCode2
	End
	Begin
		Insert INTO [dbo].[tblPraDM]
				([id_dm],
				[disciplina_modulo],
				[faltas_excesso],
				[assinatura_professor_disciplina],
				[data_assinatura],
				[cumprimento],
				[retido],
				[id_pra],
				[dmcode])
		Values (@id_dm,@disciplina_modulo,@faltas_excesso,@assinatura_professor_disciplina,@data_assinatura,@avaliaçao,@retido,@id_pra,@dmcode)
		select 1 as ReturnCode22
	END
END
GO
PRINT N'Altering [dbo].[sp_InsertDM3]...';


GO
ALTER PROCEDURE [dbo].[sp_InsertDM3]
	@id_dm int,
	@disciplina_modulo varchar(50),
	@faltas_excesso int,
	@assinatura_professor_disciplina varchar(50),
	@data_assinatura date,
	@avaliaçao varchar(50),
	@retido varchar(50),
	@id_pra int,
	@dmcode char(50)

AS
Begin
	Declare @count int
	SELECT @count= COUNT(id_dm) FROM tblPraDM Where id_dm=@id_dm

	IF(@count<>0)
	begin 
		select -1 as ReturnCode2
	End
	Begin
		Insert INTO [dbo].[tblPraDM]
				([id_dm],
				[disciplina_modulo],
				[faltas_excesso],
				[assinatura_professor_disciplina],
				[data_assinatura],
				[cumprimento],
				[retido],
				[id_pra],
				[dmcode])
		Values (@id_dm,@disciplina_modulo,@faltas_excesso,@assinatura_professor_disciplina,@data_assinatura,@avaliaçao,@retido,@id_pra,@dmcode)
		select 1 as ReturnCode23
	END
END
GO
PRINT N'Altering [dbo].[sp_InsertDM4]...';


GO
ALTER PROCEDURE [dbo].[sp_InsertDM4]
	@id_dm int,
	@disciplina_modulo varchar(50),
	@faltas_excesso int,
	@assinatura_professor_disciplina varchar(50),
	@data_assinatura date,
	@avaliaçao varchar(50),
	@retido varchar(50),
	@id_pra int,
	@dmcode char(50)

AS
Begin
	Declare @count int
	SELECT @count= COUNT(id_dm) FROM tblPraDM Where id_dm=@id_dm

	IF(@count<>0)
	begin 
		select -1 as ReturnCode2
	End
	Begin
		Insert INTO [dbo].[tblPraDM]
				([id_dm],
				[disciplina_modulo],
				[faltas_excesso],
				[assinatura_professor_disciplina],
				[data_assinatura],
				[cumprimento],
				[retido],
				[id_pra],
				[dmcode])
		Values (@id_dm,@disciplina_modulo,@faltas_excesso,@assinatura_professor_disciplina,@data_assinatura,@avaliaçao,@retido,@id_pra,@dmcode)
		select 1 as ReturnCode24
	END
END
GO
PRINT N'Altering [dbo].[sp_InsertDM5]...';


GO
ALTER PROCEDURE [dbo].[sp_InsertDM5]
	@id_dm int,
	@disciplina_modulo varchar(50),
	@faltas_excesso int,
	@assinatura_professor_disciplina varchar(50),
	@data_assinatura date,
	@avaliaçao varchar(50),
	@retido varchar(50),
	@id_pra int,
	@dmcode char(50)

AS
Begin
	Declare @count int
	SELECT @count= COUNT(id_dm) FROM tblPraDM Where id_dm=@id_dm

	IF(@count<>0)
	begin 
		select -1 as ReturnCode2
	End
	Begin
		Insert INTO [dbo].[tblPraDM]
				([id_dm],
				[disciplina_modulo],
				[faltas_excesso],
				[assinatura_professor_disciplina],
				[data_assinatura],
				[cumprimento],
				[retido],
				[id_pra],
				[dmcode])
		Values (@id_dm,@disciplina_modulo,@faltas_excesso,@assinatura_professor_disciplina,@data_assinatura,@avaliaçao,@retido,@id_pra,@dmcode)
		select 1 as ReturnCode25
	END
END
GO
PRINT N'Creating [dbo].[sp_GetDMByCode]...';


GO
CREATE PROCEDURE [dbo].[sp_GetDMByCode]
	@dmcode varchar(20)
AS
begin
		select * From tblPraDM where dmcode=@dmcode
END
GO
PRINT N'Creating [dbo].[sp_GetDMByCode2]...';


GO
CREATE PROCEDURE [dbo].[sp_GetDMByCode2]
	@dmcode varchar(20)
AS
begin
		select * From tblPraDM where dmcode=@dmcode
END
GO
PRINT N'Creating [dbo].[sp_GetDMByCode3]...';


GO
CREATE PROCEDURE [dbo].[sp_GetDMByCode3]
	@dmcode varchar(20)
AS
begin
		select * From tblPraDM where dmcode=@dmcode
END
GO
PRINT N'Creating [dbo].[sp_GetDMByCode4]...';


GO
CREATE PROCEDURE [dbo].[sp_GetDMByCode4]
	@dmcode varchar(20)
AS
begin
		select * From tblPraDM where dmcode=@dmcode
END
GO
PRINT N'Creating [dbo].[sp_GetDMByCode5]...';


GO
CREATE PROCEDURE [dbo].[sp_GetDMByCode5]
	@dmcode varchar(20)
AS
begin
		select * From tblPraDM where dmcode=@dmcode
END
GO
PRINT N'Creating [dbo].[sp_InsertPraDMLigacao]...';


GO
CREATE PROCEDURE [dbo].[sp_InsertPraDMLigacao]
	@Id_pra_dm int,
	@id_pra int,
	@id_dm int

AS
Begin
	Declare @count int
	SELECT @count= COUNT(Id_pra_dm) FROM tblPraDMLigacao Where Id_pra_dm=@Id_pra_dm

	IF(@count<>0)
	begin 
		select -1 as returnCodePraDMlig
	End
	Begin
		Insert INTO [dbo].[tblPraDMLigacao]
				([Id_pra_dm],
				[id_pra],
				[id_dm])
		Values (@Id_pra_dm,@id_pra,@id_dm)
		select 1 as returnCodePraDMlig
	END
END
GO
PRINT N'Creating [dbo].[sp_InsertPraDMLigacao2]...';


GO
CREATE PROCEDURE [dbo].[sp_InsertPraDMLigacao2]
	@Id_pra_dm int,
	@id_pra int,
	@id_dm int

AS
Begin
	Declare @count int
	SELECT @count= COUNT(Id_pra_dm) FROM tblPraDMLigacao Where Id_pra_dm=@Id_pra_dm

	IF(@count<>0)
	begin 
		select -1 as returnCodePraDMlig2
	End
	Begin
		Insert INTO [dbo].[tblPraDMLigacao]
				([Id_pra_dm],
				[id_pra],
				[id_dm])
		Values (@Id_pra_dm,@id_pra,@id_dm)
		select 1 as returnCodePraDMlig2
	END
END
GO
PRINT N'Creating [dbo].[sp_InsertPraDMLigacao3]...';


GO
CREATE PROCEDURE [dbo].[sp_InsertPraDMLigacao3]
	@Id_pra_dm int,
	@id_pra int,
	@id_dm int

AS
Begin
	Declare @count int
	SELECT @count= COUNT(Id_pra_dm) FROM tblPraDMLigacao Where Id_pra_dm=@Id_pra_dm

	IF(@count<>0)
	begin 
		select -1 as returnCodePraDMlig3
	End
	Begin
		Insert INTO [dbo].[tblPraDMLigacao]
				([Id_pra_dm],
				[id_pra],
				[id_dm])
		Values (@Id_pra_dm,@id_pra,@id_dm)
		select 1 as returnCodePraDMlig3
	END
END
GO
PRINT N'Creating [dbo].[sp_InsertPraDMLigacao4]...';


GO
CREATE PROCEDURE [dbo].[sp_InsertPraDMLigacao4]
	@Id_pra_dm int,
	@id_pra int,
	@id_dm int

AS
Begin
	Declare @count int
	SELECT @count= COUNT(Id_pra_dm) FROM tblPraDMLigacao Where Id_pra_dm=@Id_pra_dm

	IF(@count<>0)
	begin 
		select -1 as returnCodePraDMlig4
	End
	Begin
		Insert INTO [dbo].[tblPraDMLigacao]
				([Id_pra_dm],
				[id_pra],
				[id_dm])
		Values (@Id_pra_dm,@id_pra,@id_dm)
		select 1 as returnCodePraDMlig4
	END
END
GO
PRINT N'Creating [dbo].[sp_InsertPraDMLigacao5]...';


GO
CREATE PROCEDURE [dbo].[sp_InsertPraDMLigacao5]
	@Id_pra_dm int,
	@id_pra int,
	@id_dm int

AS
Begin
	Declare @count int
	SELECT @count= COUNT(Id_pra_dm) FROM tblPraDMLigacao Where Id_pra_dm=@Id_pra_dm

	IF(@count<>0)
	begin 
		select -1 as returnCodePraDMlig5
	End
	Begin
		Insert INTO [dbo].[tblPraDMLigacao]
				([Id_pra_dm],
				[id_pra],
				[id_dm])
		Values (@Id_pra_dm,@id_pra,@id_dm)
		select 1 as returnCodePraDMlig5
	END
END
GO
PRINT N'Update complete.';


GO
